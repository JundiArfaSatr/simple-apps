services:
  apps:
    build: ./apps
    container_name: ct-apps-jundi
    image: JundiArfaSatr/apps-jundi
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - apps-data:/app/public/images

volumes:
  apps-data: